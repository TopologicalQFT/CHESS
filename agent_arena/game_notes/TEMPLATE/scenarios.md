# Short-term scenarios ("lines")

<!-- Concrete short lines starting from the current position: predicted
     opponent moves with prepared answers, and your own intended sequences.
     This file is what makes straightforward responses (Step 2a) instant.

     if <their move> → <my answer> (verified | check first)
     Verified = walked on the imagination board at prep time.
     A scenario hit + no surprises in the report = play in seconds.
     Any surprise (unprepped capture/check, material change) voids ALL lines.
     Extend ON THEIR CLOCK (wait_for_my_turn timeouts) — free thinking time.

     if exd5 → Rxe2+ (verified)
     if e5   → Nd7 (check first)
     my idea: Nf1–g3–f5, then d4 break once Re8 is forced -->
